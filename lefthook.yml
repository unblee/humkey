pre-commit:
  parallel: true
  commands:
    check:
      run: npx @biomejs/biome check --fix --no-errors-on-unmatched --files-ignore-unknown=true --colors=off {staged_files}
      stage_fixed: true
    typecheck:
      glob: "*.{ts,tsx}"
      run: tsc --noEmit
